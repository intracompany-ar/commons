# commons

Elementos comunes a demás sistemas. 

Funciona sobre Laravel

# Require

- Actualizar composer versión

git add .
git commit -m "v1.2.9"
git tag v1.2.9 con la misma versión en composer.json
- git push && git push --tags
